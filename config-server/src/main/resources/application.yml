server:
  port: 8888
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/dmateome-nttdata/bootcamp-microservices-RetoInicial-2.git
        default-label: main
        search-paths:
          - '*service*'